#include <stdlib.h>
#include <stdio.h>

int main()
{
    /*
    below are the steps describing the functionalities 
    in order to get this assignment done
    
    1. receive string, encode it using the algorithm
    2. serialize the tree used
    3. decode from serialized tree and code
    4. interactions from terminal (save enconding, reading from file, etc)
    */

    exit(EXIT_SUCCESS);
}